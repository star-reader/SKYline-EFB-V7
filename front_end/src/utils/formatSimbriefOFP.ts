export default (d: any) => {
    return {
        'general': d.general,
        'origin': d.origin,
        'destination': d.destination,
        'aircraft': d.aircraft,
        'fuel': d.fuel,
        'weights': d.weights
    }
}